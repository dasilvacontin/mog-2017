const Battle = require('./Battle.js')
const b = new Battle ()

/*
const player = { x: 100, y: 150 }

const { x, y } = player
const x = player.x
const y = player.y
const fighters = [f0, f1, f2, f3, f4]

const [x, y] = pos
const [first] = fighters
// const first = fighters[0]

player.x, player.y

class GameObject {
    logic () {

    }
}

const gameObjects = []
gameObjects.forEach((gameObject) => {
    gameObject.logic()
})
*/